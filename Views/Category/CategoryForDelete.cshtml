@model CategoryModel

@* <form asp-controller="Home" asp-action="ProductForDelete">
    <button type="submit" class="btn btn-danger">Borrar</button>
    <a class="btn btn-secundary" asp-action="Producto" asp-controller="Home">
        Regresar
    </a>

</form> *@


<h2>¿Está seguro de que desea eliminar esta categoria?</h2>

<div>
    <h4>@Model.Name</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Descripción
        </dt>
        <dd class="col-sm-10">
            @Model.Descripcion
        </dd>
        
    </dl>
</div>

<form asp-action="CategoryDelete" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Borrar</button>
    <a class="btn btn-secondary" asp-action="CategoryList">Cancelar</a>
</form>